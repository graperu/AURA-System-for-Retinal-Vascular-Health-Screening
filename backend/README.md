# AURA Backend

This directory is reserved for the Java 21 and Spring Boot 3 modular monolith.
Application initialization, PostgreSQL configuration, and Flyway begin in
Phase 2. Authentication and RBAC follow in Phase 3.

The backend is the only public API used by the frontend and the only component
allowed to access private storage credentials.

