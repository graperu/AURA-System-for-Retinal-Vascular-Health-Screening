# NHẬT KÝ CÔNG VIỆC DỰ ÁN AURA (WORKLOG)
**Ngày thực hiện**: 31/08/2026  
**Dự án**: AURA - Retinal Vascular Health Screening System  
**Mục tiêu chính**: Khảo sát & Audit toàn diện hệ thống theo chuẩn Đề bài, Xử lý triệt để dữ liệu Mock/Fake, Khắc phục lỗi giao diện và Hoàn thành nhiệm vụ an toàn y khoa P0-1.

---

## I. TỔNG HỢP CÁC CÔNG VIỆC ĐÃ HOÀN THÀNH TRONG NGÀY

### 1. Audit Toàn Diện Hệ Thống & Chuẩn Hóa Baseline (`AUDIT_REPORT.md`)
- **Đối chiếu chuẩn mực**: Rà soát 39 Yêu cầu chức năng (**FR-1 $\rightarrow$ FR-39**) và 23 Yêu cầu phi chức năng (**NFR-1 $\rightarrow$ NFR-23**) theo tài liệu `docs/01-requirements/software-requirements-specification.md` (chuẩn IEEE Std 830-1998, căn cứ `DEBAI.pdf`).
- **Phân loại hiện trạng chính xác**:
  - **39 FR**: PASS: 10 (25.6%) | PARTIAL: 12 (30.8%) | MOCK: 4 (10.3%) | UI ONLY: 10 (25.6%) | FAIL: 2 (5.1%) | MISSING: 1 (2.6%).
  - **23 NFR**: PASS: 7 (30.4%) | PARTIAL: 9 (39.1%) | MOCK: 2 (8.7%) | FAIL: 1 (4.3%) | MISSING: 1 (4.3%) | NOT VERIFIED: 3 (13.1%).
- **Kiểm thử thực nghiệm & Bằng chứng thực tế**:
  - Kiểm thử Chat 2 chiều Bệnh nhân - Bác sĩ với timestamp: `TEST-PATIENT-20260831-203418` và `TEST-DOCTOR-20260831-203418`, kiểm chứng lưu trữ 4 dòng trong bảng `chat_messages` (PostgreSQL) và tồn tại sau `docker restart aura-backend`.
  - Kiểm tra Authorization: Phát hiện tài khoản lạ `attacker2@aura.com` gửi tin nhắn cho Bệnh nhân do thiếu kiểm tra phân công (`FAIL / NOT VERIFIED`).
  - Thẩm định AI Engine: Xác nhận `ai-service/` chưa có tệp trọng số PyTorch `.pth` và hàm `generate_gradcam_heatmap` dùng `cv2.GaussianBlur` tâm cố định.
  - Ghi nhận thông tin Docker thật (4 containers: `aura-postgres`, `aura-backend`, `aura-ai-service`, `aura-frontend`).

---

### 2. Khắc Phục Lỗi Giao Diện & Trải Nghiệm Người Dùng (UI/UX)
- **Lỗi đã sửa**: Giao diện Modal xem báo cáo chi tiết và CDS Viewer bị che khuất nút đóng khi cuộn xuống dưới, không có lối thoát ra.
- **Giải pháp triển khai**:
  - Tạo Header cố định (`Sticky Header`) ở đầu modal chứa tiêu đề và nút **"Đóng (ESC)"** nổi bật.
  - Bổ sung phím tắt `Escape` cho phép người dùng bấm phím để đóng modal tức thì.
  - Đã kiểm thử ghi hình tự động bằng trình duyệt xác nhận hoạt động mượt mà.

---

### 3. Xử Lý Triệt Để Dữ Liệu Fake & Đấu Nối Database Thật
- Chuyển đổi luồng tạo ca khám từ Mock sang lưu trực tiếp vào PostgreSQL bảng `screenings`.
- Cập nhật định tuyến Nginx Reverse Proxy `/api/v1` và `/ai` đảm bảo Frontend giao tiếp thông suốt với Backend (Port `8081`) và AI Core (Port `8000`).

---

### 4. Hoàn Thành Nhiệm Vụ P0-1: Loại Bỏ Fallback Y Tế Giả Khi AI Offline
> **Nguyên tắc an toàn y tế**: Khi máy chủ AI gặp sự cố hoặc ngoại lệ kết nối, tuyệt đối **KHÔNG** được tự động sinh kết quả chẩn đoán giả (`HIGH / 0.94`). Hệ thống phải lưu trạng thái `FAILED`, đặt `null` cho các trường nguy cơ/độ tin cậy và bảo toàn ảnh gốc của bệnh nhân.

- **Các bước kỹ thuật đã thực hiện**:
  1. **Enum `ScreeningStatus`**: Thêm giá trị `FAILED` ([ScreeningStatus.java](file:///d:/VERSIONPLUS/AURA-System-for-Retinal-Vascular-Health-Screening/backend/src/main/java/com/aura/screening/entity/ScreeningStatus.java)).
  2. **Flyway Migration `V011`**: Tạo file `V011__add_failed_to_screening_status_check.sql` cập nhật Check Constraint trong PostgreSQL:
     ```sql
     ALTER TABLE screenings DROP CONSTRAINT IF EXISTS screenings_status_check;
     ALTER TABLE screenings ADD CONSTRAINT screenings_status_check CHECK (status IN ('PENDING', 'ANALYZED', 'REVIEWED', 'FAILED'));
     ```
  3. **Logic `ScreeningService.java`**:
     - Xóa bỏ hoàn toàn code gán fallback giả: `calculatedRisk = RiskLevel.HIGH; confidence = 0.94; findings = "Phát hiện co thắt tiểu động mạch...";`.
     - Khối `catch` khi AI offline / lỗi: Gán `status = FAILED`, `riskLevel = null`, `confidence = null`, `findings = "Không thể kết nối đến máy chủ phân tích AI. Ảnh chụp võng mạc đã được lưu trữ an toàn để thẩm định lại."`, giữ nguyên `imageUrl`.
     - Tối ưu gọi HTTP sang FastAPI bằng `java.net.http.HttpClient` với giao thức `HTTP_1_1` và JSON serialization chuẩn Jackson.
  4. **Kiểm thử tự động**: Viết `ScreeningServiceTest.java` $\rightarrow$ Chạy `mvn test` đạt **33/33 Tests PASS** (0 Failures, 0 Errors).
  5. **Kiểm thử thực nghiệm Live E2E**:
     - Khi AI Online: API trả về `status: ANALYZED`, `riskLevel: HIGH`, `confidence: 0.92`.
     - Khi AI Offline (`docker stop aura-ai-service`): API trả về `status: FAILED`, `riskLevel: null`, `confidence: null`, ảnh được lưu trữ nguyên vẹn trong PostgreSQL.

---

## II. BẢNG TRẠNG THÁI KIỂM CHỨNG TRONG DATABASE (POSTGRESQL)

```sql
SELECT id, status, risk_level, confidence, SUBSTRING(findings, 1, 55) as findings_summary, SUBSTRING(image_url, 1, 30) as img_prefix 
FROM screenings 
ORDER BY created_at DESC LIMIT 2;
```

```text
                  id                  |  status  | risk_level | confidence |                    findings_summary                     |           img_prefix           
--------------------------------------+----------+------------+------------+---------------------------------------------------------+--------------------------------
 4efc2187-e816-4d21-87ff-e030ef515836 | FAILED   |            |            | Không thể kết nối đến máy chủ phân tích AI. Ảnh chụp võ | data:image/png;base64,iVBORw0K
 c2aa576d-c9c0-4924-a3e8-45d98e97b9e2 | ANALYZED | HIGH       |       0.92 | Cấu trúc vi mạch võng mạc được phân tích bởi mô hình AU | data:image/png;base64,iVBORw0K
```

---

## III. CÁC TỆP TIN ĐÃ TẠO MỚI / CHỈNH SỬA CHÍNH

1. `AUDIT_REPORT.md` (Tạo mới & cập nhật toàn bộ báo cáo audit baseline 39 FR / 23 NFR).
2. `backend/src/main/java/com/aura/screening/entity/ScreeningStatus.java` (Thêm enum `FAILED`).
3. `backend/src/main/resources/db/migration/V011__add_failed_to_screening_status_check.sql` (Cập nhật check constraint).
4. `backend/src/main/java/com/aura/screening/service/ScreeningService.java` (Sửa logic gọi AI & loại bỏ fallback giả).
5. `backend/src/test/java/com/aura/screening/service/ScreeningServiceTest.java` (Unit test cho P0-1).
6. `frontend/src/components/ReportModal.tsx` & `frontend/src/components/CDSViewerModal.tsx` (Sửa nút đóng và header).
7. `walkthrough.md` (Báo cáo walkthrough chi tiết cho P0-1).

---

## IV. HẠNG MỤC KẾ TIẾP ĐÃ LÊN KẾ HOẠCH
- [ ] **P0-2**: Khắc phục lỗ hổng IDOR tại `GET /api/v1/screenings/{id}` (kiểm tra quyền sở hữu ca khám của bệnh nhân/bác sĩ).
- [ ] **P0-3**: Xây dựng bảng `doctor_patient_assignments` và phân quyền ca khám cho bác sĩ.
- [ ] **P0-4**: Tách riêng bảng kết quả AI và kết quả bác sĩ hiệu chỉnh để không ghi đè dữ liệu gốc.
