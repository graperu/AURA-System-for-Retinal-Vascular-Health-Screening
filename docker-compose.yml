name: aura

# Services will be added in their implementation phases, after each service has
# a buildable application and Dockerfile. Keeping this file valid but empty
# avoids presenting non-runnable placeholder containers as completed services.
services: {}
