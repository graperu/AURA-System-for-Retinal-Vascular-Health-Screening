# Infrastructure

Deployment definitions will be introduced with the services they operate.
Production requires HTTPS, private storage, restricted CORS, least-privilege
database access, protected internal AI communication, and externally managed
secrets.

No cloud resource is provisioned in Phase 1.

