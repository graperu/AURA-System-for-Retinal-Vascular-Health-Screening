# API documentation

Backend APIs use the `/api/v1` prefix and a consistent success/error envelope.
The internal AI service exposes `/health` and `/predict`. Exact contracts are
defined before implementation and versioned alongside tests.

