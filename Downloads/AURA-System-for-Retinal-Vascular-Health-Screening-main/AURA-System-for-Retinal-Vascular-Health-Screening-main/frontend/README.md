# AURA Frontend

This directory is reserved for the React, TypeScript, and Vite application.
Application initialization belongs to Phase 5.

Only public configuration prefixed with `VITE_` may be exposed here. The
frontend must not contain database, Supabase service-role, JWT, or internal AI
credentials.

